#ifndef CS706_COMMON_H
#define CS706_COMMON_H
#include <vector>
#include <map>
#include <list>
#include <queue>
#include <set>
#include <algorithm>
#include <iostream>
#include <cstring>
#include <string>
#include <cstdlib>
#include <complex>
#include <boost/foreach.hpp>
#include <cassert>
#include <cstdint>
#include <climits>

#define foreach BOOST_FOREACH

using std::vector;
using std::map;
using std::list;
using std::set;
using std::pair;
typedef vector<int> vi;
typedef vector<vi> vvii;
typedef map<int, int> mii;
typedef map<int, mii> mimii;
typedef map<mii, int> mmiii;
typedef vector<mmiii> vmmiii;
typedef vector<mii> vmii;
typedef vector<mimii> vmimii;
typedef set<int> si;
typedef vector<si> vsi;
typedef pair<int, int> pii;
typedef pair<pii, int> ppiii;
typedef pair<int, pii> pipii;
typedef pair<mii, int> pmiii;
typedef pair<int, mii> pimii;
typedef vector<pii> vpii;
typedef map<pii, int> mpiii;
typedef map<int, pii> mipii;

#endif /* CS706_COMMON_H */
